<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sg.edu.rp.desmond.fypaircontrollers">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GateActivity" />
        <activity android:name=".LoginActivity" />
        <!-- <activity android:name=".Chat" /> -->
        <!-- <activity android:name=".Users" /> -->
        <activity android:name=".GateSingleActivity" />
        <activity android:name=".ManageGate" />
        <activity android:name=".TimeSingleActivity" />
        <activity android:name=".ManageTimeSlot" />
        <activity android:name=".SingleDateActivity"/>
        <activity android:name=".ManageFlight"/>
    </application>

</manifest>